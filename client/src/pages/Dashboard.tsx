/*
purpose: the main dashboard showing current turbine status (latest telemetry) and any active alarms
what will be added later:
- fetch turbine status and active alarms from the api
- display turbine cards with key metrics
- show an alarm list
*/

import React from 'react';

//fetches urbine status & active alarms from the API
//renders turbine cards & an alert list
const Dashboard: React.FC = () => {
    return <div>Dashboard placeholder</div>; //placeholder - will be replaced with the actual dashboard ui
};

export default Dashboard;