/*
purpose: centralises all API calls to the backend, making it easy for react components to fetch data
what will be added later:
-implement each function using axios (or fetch)
- include error handling
- export them for use in components
*/

// axios functions for all api endpoints.
// will include:
// - getTurbines()
// - getCurrentStatus()
// - getActiveAlarms()
// - getTelemetry(turbineId, from, to)
// - uploadFile(file)
// - getConfig()
// - updateConfig(config)
// - exportDatabase()

// each function will later use axios to call the corresponding api route